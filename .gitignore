# ── Node.js ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Build output ──
dist-ssr/
build/

# ── Vite ──
.vite/

# ── Analizador C — archivos intermedios generados por Flex/Bison ──
analizador/lex.yy.c
analizador/sql_parser.tab.c
analizador/sql_parser.tab.h
analizador/*.o
analizador/*.out
analizador/stack.hh
analizador/location.hh
# validador.exe y validador se commitean para que usuarios Windows puedan correr sin compilar

# ── Variables de entorno ──
.env
.env.local
.env.*.local

# ── Logs ──
logs/
*.log

# ── Editor ──
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Cache ──
.cache/

